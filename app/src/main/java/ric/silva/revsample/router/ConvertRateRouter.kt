package ric.silva.revsample.router

import ric.silva.revsample.ConverterContract

class ConvertRateRouter(view: ConverterContract.View?) : ConverterContract.Router {

    override fun goBack() {
        // This would have a back navigation
    }
}